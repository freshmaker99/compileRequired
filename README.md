use $ `npm run build` to generate code



builder is inside builder folder

based on settings in package.json (node builder/run src/ index.js lib/js/main.js) index from src folder is compiled to lib/js/main.js